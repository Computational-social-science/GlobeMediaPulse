# ==========================================
# Core Backend Framework & Server
# ==========================================
fastapi>=0.100.0            # High-performance async web framework for building APIs
uvicorn[standard]>=0.20.0   # ASGI server to run the FastAPI application (production-ready)
pydantic>=2.0.0             # Data validation and settings management using Python type hints
pydantic-settings>=2.0.0    # Settings management support for Pydantic (loading .env)
python-dotenv>=1.0.0        # Reads key-value pairs from .env file and sets them as environment variables

# ==========================================
# Networking & HTTP Clients
# ==========================================
aiohttp>=3.9.0              # Asynchronous HTTP client/server framework (used for high-concurrency requests)
httpx>=0.27.0               # Modern, fully featured HTTP client for Python 3 (sync & async support)
requests>=2.31.0            # Simple, elegant HTTP library (synchronous fallback)
fake-useragent>=1.5.0       # Generates random User-Agent strings to bypass basic scraping blocks

# ==========================================
# Content Extraction & Web Scraping
# ==========================================
trafilatura>=1.6.0          # State-of-the-art library for extracting main text/metadata from HTML
playwright>=1.41.0          # Headless browser automation (handling JS-heavy sites)
playwright-stealth>=1.0.6   # Plugin to make Playwright harder to detect by anti-bot systems
beautifulsoup4>=4.12.0      # Library for pulling data out of HTML and XML files (used by Trafilatura)
lxml>=4.9.0                 # High-performance HTML/XML processing library (dependency for Trafilatura)

# ==========================================
# Database & Storage
# ==========================================
psycopg2-binary>=2.9.9      # PostgreSQL database adapter for Python
sqlalchemy>=2.0.0           # SQL Toolkit and Object Relational Mapper (ORM)
alembic>=1.13.0             # Database migration tool for SQLAlchemy
redis>=5.0.0                # Python client for Redis key-value store (used for queues/caching)

# ==========================================
# NLP (Natural Language Processing) & Geo
# ==========================================
nltk>=3.8.1                 # Natural Language Toolkit (tokenization, NER, etc.)
wordfreq>=3.0.0             # Access to word frequency data (used for error detection logic)
wn>=0.0.23                  # WordNet interface (dependency for some NLP tasks)
rapidfuzz>=3.0.0            # Rapid fuzzy string matching (Levenshtein distance optimization)
symspellpy>=6.7.0           # Fast spelling correction and fuzzy search library
pycountry>=22.3.5           # ISO country codes and names (for geolocation mapping)
geopy>=2.4.0                # Geocoding library (converting addresses to coordinates)

# ==========================================
# Data Analysis & Math
# ==========================================
pandas>=2.0.0               # Powerful data structures for data analysis (Time series, DataFrames)
numpy>=1.24.0               # Fundamental package for scientific computing (array processing)
scikit-learn>=1.3.0         # Machine learning library (used for clustering/classification)
ruptures>=1.1.9             # Change point detection in time series data (trend analysis)

# ==========================================
# Development & Testing
# ==========================================
pytest>=7.0.0               # Framework for writing and executing tests
black>=23.0.0               # Uncompromising code formatter (ensures consistent style)
isort>=5.0.0                # Utility to sort imports alphabetically
