# Development Dockerfile for Backend
# Optimized for fast rebuilds and hot reloading
FROM python:3.11-slim

# Set environment variables
ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1
ENV PYTHONPATH=/app

WORKDIR /app

# Install system dependencies
# gcc and build-essential for compiling some python packages
# curl for healthchecks
RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc \
    build-essential \
    curl \
    git \
    && rm -rf /var/lib/apt/lists/*

# Copy requirements file
COPY backend/requirements.txt .

# Install dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Install dev dependencies (optional, e.g., for testing)
# RUN pip install pytest black isort

# Note: We do NOT copy source code here.
# Source code will be mounted as a volume in docker-compose.yml

# Expose port
EXPOSE 8002

# Command (Overridden by docker-compose)
CMD ["uvicorn", "backend.main:app", "--host", "0.0.0.0", "--port", "8002", "--reload"]
